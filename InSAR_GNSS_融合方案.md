# 边坡变形监测：InSAR（天）+ GNSS（地）× DL-Geo 算法融合方案

> 目标：把"天空的 InSAR 面域覆盖"和"地面的 GNSS 高频精准点"接到 DL-Geo 现有算法体系（①-⑧ + ARIMA+Kalman）上，
> 形成"数据 → 融合 → 预测 → 风险分级预警"的完整链路。所有设计沿用项目既有工程标准：防泄漏流水线、
> 速率+累积两级口径、验证段选参、消融实验。

---

## 1. 总体思路：天给"场"，地给"过程"，算法给"未来"，预警给"分级"

| 角色 | 数据源 | 回答的问题 |
|---|---|---|
| 面 | InSAR（PS/DS 点，升/降轨） | 变形**发生在哪里**、空间范围多大、哪个区域在加速 |
| 点 | GNSS 站（坡顶/坡腰/坡脚 + 区外稳定站） | 变形**现在多快**、三维真值、绝对基准 |
| 未来 | DL-Geo 算法①-⑧ | 下一步多快、区间多大、到阈值概率多少 |
| 决策 | 分级阈值 + 归因 | 什么时候、因为什么、该做什么等级的响应 |

两类数据天然互补：InSAR **空间密、时间稀**（Sentinel-1 约 12 天一景）、只有 LOS 一维投影、相对基准；
GNSS **空间稀、时间密**（日解甚至小时解）、三维真值、绝对基准。融合的本质就是拿 GNSS 补 InSAR 的
时间分辨率和基准，拿 InSAR 补 GNSS 的空间覆盖。

---

## 2. 数据特性对比（决定谁信谁、什么时候信）

| 维度 | InSAR（SBAS/PS） | GNSS |
|---|---|---|
| 空间密度 | 每 km² 数十~数百 PS/DS 点 | 每站成本高，边坡上通常 3~8 个点 |
| 时间采样 | 12 天（单轨）/ 6 天（双轨组合） | 日解（可加密至小时解） |
| 观测量 | LOS 方向一维位移投影 | ENU 三维 |
| 基准 | 相对（参考点选取敏感） | 绝对（ITRF） |
| 典型精度 | 2~5 mm（受大气延迟影响大） | 水平 1~2 mm、垂直 2~4 mm（日解） |
| 主要误差 | 对流层延迟（与降雨/高程相关！）、失相干、几何畸变 | 多路径、天线稳定度、对中误差 |
| 边坡上的坑 | 陡坡 layover/shadow、植被区相干性差、雨季掉点 | 点位只能放在可达处，未必在最危险部位 |

**由此推出两条铁律：**
1. **精度分层信任**——融合层的观测噪声 R 必须按源分开设置：GNSS 的 R 小、InSAR 的 R 大且随相干性/大气改正残差自适应放大（直接复用 ARIMA+Kalman 文章里的"少信这个观测"逻辑）；
2. **InSAR 大气误差与降雨同源**——边坡预警恰恰最关心降雨时段，所以 InSAR 时序必须先做大气改正（GACOS/ERA5 或经验高程相关项改正），并在融合层保留"降雨期间降权"的开关。

---

## 3. 总体架构：五层

```
L4 决策层    分级预警（速率/加速度分级 + 超越概率）+ SHAP 归因          ← ④⑤
             ↑
L3 预测层    单点：ARIMA+Kalman 在线 / ①LSTM / ③CNN-Transformer / ⑧基础模型
             面域：②GCN 时空图预测；物理：⑥PINN / ⑦神经算子
             ↑
L2 融合层    状态空间多源同化（Kalman，异步更新）+ 空间插值（Kriging/GCN）
             ——输出：每个关键点的"融合三维位移+速度"统一时序（带方差）
             ↑
L1 对齐层    基准统一 / LOS→三维分解 / 时间轴对齐 / 质量标记
             ↑
L0 数据层    GNSS 解算日解(ENU+方差)；InSAR 处理链→PS/DS 点表；雨量/库水位/温度
```

关键原则：**L3 的所有算法只吃 L2 的统一输出**，不直接吃原始 InSAR/GNSS——这样 ①③⑧ 的
单点模型、② 的图模型接口保持不变（符合现有 Notebook §2.2 配置区的 `DATA_PATH / FEATURE_COLS /
TARGET_COL` 约定）。

---

## 4. L0/L1 数据规格与对齐（先干这个，周期最长的先启动）

### 4.1 交付物规格

| 数据 | 规格 | 格式 |
|---|---|---|
| GNSS | 每站日解 ENU 时序 + 1σ 方差；含 1~2 个**变形区外稳定参考站** | 长表 `station, date, E, N, U, sE, sN, sU` |
| InSAR | PS/DS 点表：坐标(E/N 或 UTM)、**LOS 单位向量(ENU)**、LOS 位移时序、相干性、年均速率 | 点表 + 宽表时序矩阵 |
| 环境量 | 小时雨量、日温度；库岸边坡加日库水位 | 长表 `time, value` |
| 地质 | 剖面、滑面深度（钻孔）、岩性、坡向坡度（DEM 导出） | 矢量/栅格 |

### 4.2 对齐层四件事（每件都有防泄漏红线）

1. **空间基准统一**：全部转 UTM；InSAR 参考点选在变形区外、且与 GNSS 稳定站重合或邻近——用稳定站对 InSAR 时序做参考基准校准（减去稳定站均值）。
2. **LOS→三维分解**：单轨只有一维。升/降轨联合可解 E、U 两分量（N 方向病态，需 GNSS 约束或先验水平:垂直比）；工程简化路线：**以 GNSS 站为锚，回归拟合 LOS 与 GNSS 垂直分量的投影关系，反演全区"等效垂直位移"**——精度不够做绝对值，做趋势和空间相对变形足够。
3. **时间轴对齐**：GNSS 降为日解；InSAR 12 天时序**只允许用过去观测**做因果插值（三次样条向前看一帧就是泄漏——这是 L1 层最大的坑）；对齐后输出统一日粒度宽表 + 每格一个"来源与质量标记"（G=GNSS 直接观测 / I=InSAR 因果插值 / F=融合估计）。
4. **质量标记**：相干性 <0.4、雨季掉点、GNSS 缺测，都进标记列，下游融合层据此放大 R。

---

## 5. L2 融合核心：异步状态空间同化（ARIMA+Kalman 文章的直接延伸）

刚写的 ARIMA+Kalman 里两条纪律——"少信异常观测（当步门控）"和"结构变化放大 Q"——在多源融合里**原样升级为层核心**，只是状态和观测矩阵变了：

**状态**（每个关键点）：`x = [uE, uN, uU, vE, vN, vU]`（三维位移+三维速度），匀速转移

```
F = [[I, Δt·I], [0, I]],   Q = diag(q_u, q_v)（蠕变加速期放大）
```

**观测**（异步到达，来什么更新什么）：

```
GNSS 日解到达:   z = [E,N,U]ᵀ,            H = [I₃, 0],      R = R_gnss（小）
InSAR 景到达:    z = l·[uE,uN,uU]ᵀ,       H = [l₁I? 简化为 l·I₃ 投影],  R = R_insar/相干性加权（大）
                 （l 为该点 LOS 单位向量；残差 |r|>3σ̂_rob → 当步门控 0.2）
```

- 12 天 vs 1 天的采样率不匹配，在"预测→观测到达→更新"的异步循环里**天然消解**，不需要重采样凑齐；
- InSAR 大气残余误差是孤立尖峰 → 走异常门控；雨季连续掉点/失相干 → 该点 R 持续放大；
- 滑坡进入加速蠕变（连续同向加速）→ Q 放大让速度状态快速跟上——这正是文章里 b1/b2 触发器的物理对应；
- 输出三列：位移（融合）、速度（融合）、方差（置信度），供 L3 全体算法和 L4 预警使用。

**注意区分两个 Kalman 的角色，别混**：L2 的融合 Kalman 状态是"三维位移/速度"（多源观测同化）；
ARIMA+Kalman 文章里的残差 Kalman 状态是"模型偏差"（L3 在线纠偏）。一个在 L2 吃观测，一个在 L3 吃模型残差，都要有，但各司其职。

---

## 6. 算法逐个落位（现有 ①-⑧ + ARIMA+Kalman）

| # | 算法（现状） | 在本方案中的角色 | 输入 | 模板改动量 |
|---|---|---|---|---|
| ★ | **ARIMA+Kalman（已做）** | **GNSS 日序在线一级预警**：ARIMAX(降雨外生) 基线 + 残差纠偏，生产环境守门员；其门控/Q逻辑升级为 L2 融合核 | L2 融合速度序列 + 降雨 | 几乎为零，改数据路径 |
| ① | LSTM（单点时序） | 各 GNSS 站的位移/速率预测主模型；雨量、温度作外生特征 | L2 单点长表 + 环境外生 | 仅 §2.2 配置 |
| ② | GCN（多点时空图） | **InSAR 的主战场**：PS/DS 点做图节点（KNN+对称归一化已实现），GNSS 站做高置信锚节点（边上加权）；预测面域变形场，重点输出"哪些区域在加速" | 多测点长表 + 坐标表（PS 子集 + GNSS） | 节点特征加质量/来源标记；异质节点加权 |
| ③ | CNN-Transformer（单点混合） | 对重点断面 GNSS 站做长依赖预测；注意力-滞后曲线对照**降雨-位移滞后核**（已有对照经验，边坡同样适用：降雨→孔压→位移的滞后） | 同① | 仅 §2.2 配置 |
| ④ | XGBoost+SHAP（待做④） | 风险归因：特征 = 速率/加速度、累积量、降雨强度/历时、库水位变幅、InSAR 面域平均速率、相干性变化率、距坡缘距离……输出"这次加速主要因为什么" | L2 宽表 + 环境量 | 新 Notebook，按既有标准 |
| ⑤ | 分位数/Conformal（待做⑤） | 预警区间：预测分位数 + Conformal 校准；**InSAR-GNSS 分歧度、门控触发率作为不确定性放大器**，分歧大自动加宽区间 | L3 各模型输出 + L2 方差 | 新 Notebook |
| ⑥ | PINN 太沙基固结（待做⑥） | 物理约束层改造为**边坡蠕变**：蠕变三阶段经验模型/渗流-应力简化物理作为软约束；用 InSAR 面域场反演蠕变参数（空间分布的参数图） | L2 面域场 + 地质参数先验 | 物理项替换 |
| ⑦ | 神经算子 FNO/DeepONet（待做⑦） | FEM 代理：降雨-渗流-位移场正问题代理，替代昂贵数值模拟做 what-if（不同降雨情景下的面域响应） | 模拟数据训练 + 实测校验 | 新 Notebook |
| ⑧ | 时序基础模型 Chronos/TimesFM（待做⑧） | 新增测点/新边坡 zero-shot 冷启动：没有足够历史的新 GNSS 点直接 zero-shot 出基线预测，再被残差纠偏层接管 | L2 单点序列 | 仅推理接口 |

**落地优先级**：★（现成）→ ①（现成）→ ②（InSAR 数据到位后价值最大）→ ④⑤（预警体系）→ ③⑧ → ⑥⑦。

---

## 7. L4 风险预警层（⑤的工程化）

1. **双指标口径**（与现有两级评估标准一致）：预警看**速率/加速度**（时效性），复盘看**累积位移**（总量与历史对比）；
2. **分级参考**（经验速度分级，阈值按边坡等级标定）：
   - 速率 < 2 mm/d：稳定监测（周报）
   - 2~10 mm/d：缓慢变形（加密关注，黄色）
   - 10~50 mm/d 且加速度>0：加速变形（橙色，启动加密观测）
   - >50 mm/d 或逆速度收敛：临滑风险（红色，应急）
3. **逆速度法**（Fukuzono）：1/v 对时间线性收敛 → 外推失稳时间 TTA，配合⑤给出 TTA 的分位数区间；
4. **多源一致性检查作为预警前置闸门**：InSAR 与 GNSS 分歧超限 → 先查大气/基准/失相干，再触发预警，避免误报；分歧本身进⑤的区间放大器；
5. 归因报告：每次升级预警自动跑④的 SHAP，输出"主要驱动因子 Top-5"。

---

## 8. 分阶段实施路线

| 阶段 | 内容 | 前置数据 | 验收标准 |
|---|---|---|---|
| 0 摸底 | 1 年 Sentinel-1 双轨时序（SBAS 处理成 PS/DS 点表）+ 3~5 个 GNSS 站日解 + 雨量 | — | InSAR 点密度、稳定站速率 <1 mm/a、时间覆盖率 >80% |
| 1 对齐+在线 | L1 对齐层；★ARIMA+Kalman 直接接到 GNSS 日序（降雨外生）；L2 融合 Kalman 原型 | 阶段0 | 防泄漏检查通过；融合序列在 GNSS 空窗期的插值误差 < GNSS 日解 1σ；异常点门控命中率抽检 |
| 2 面域预测 | ②GCN 上 InSAR 点集（含锚节点加权）；①/③ 单点模型换真实数据 | 阶段1 | 速率口径测试段 MAE 优于基线（ARIMA+Kalman）；消融：无图/无锚节点对照 |
| 3 风险体系 | ④SHAP 归因 + ⑤分位数/Conformal + L4 分级阈值 | 阶段2 | 区间覆盖率达标（90% 区间实测覆盖 87~93%）；预警回测误报/漏报统计 |
| 4 物理+代理 | ⑥蠕变 PINN 参数反演、⑦神经算子 what-if、⑧新点冷启动 | 阶段2 | 物理一致性（参数图与地质剖面不矛盾）；zero-shot 优于朴素基线 |

---

## 9. 已知坑与对策（按踩到概率排序）

| 坑 | 对策 |
|---|---|
| InSAR 大气延迟与降雨同源，雨季"假变形" | 大气改正（GACOS/ERA5）必做；融合层保留"强降雨日 InSAR 降权"开关；GNSS 为准 |
| 陡坡几何畸变（layover/shadow）导致坡面点缺失或失真 | 处理链做几何畸变掩膜；危险部位不依赖 InSAR，用 GNSS/其他地面手段补 |
| 植被覆盖区相干性差、雨季掉点 | 用 DS（分布式散射体）处理；L1 质量标记 → 融合层 R 放大 |
| InSAR 相对基准漂移 | 变形区外稳定 GNSS 站做锚，每景校准 |
| LOS 一维 vs 三维需求 | 升降轨联合 + GNSS 锚点反演等效垂直分量；报告口径统一为"等效垂直/沿坡向" |
| 12 天 vs 1 天采样不匹配 | 不重采样凑齐，用 L2 异步 Kalman 同化 |
| 累积位移受季节温度循环污染 | STL/趋势项分离后再进模型；预警只看趋势+速率 |
| 对齐层插值引入未来信息（泄漏） | L1 只许因果插值；数据质量标记列随数据一起下发 |
| GNSS 多路径/天线扰动 | 稳定站差分；残差尖峰走门控 |

---

## 10. 与现有代码库的衔接清单

- `ARIMA-Kalman/demo_arima_kalman.py`：`DATA` 段换成 GNSS 站长表，`temp` 外生换成降雨 → 即为阶段1的在线预警原型；
- `LSTM/`、`CNN-Transformer/`：§2.2 配置区 `DATA_PATH`→GNSS/融合长表，`TARGET_COL`→融合速度列（速率目标+累积重构口径现成）；
- `GCN/`：多测点长表 = InSAR PS/DS 子集（每 50~100 m 取点）+ GNSS 站；坐标表 + 节点来源标记列新增；建图仍用 KNN，锚节点边权按精度比加权（小改动）；
- 新增 `04_XGBoost_SHAP/`、`05_Conformal_预警/` 按 README 既有路线；
- 全部沿用：训练段 fit / 按目标时刻划分、速率+累积两级、消融、验证段选参。

---

## 附：最小可行起步（本周就能做）

1. 找一个有历史的边坡案例数据（公开的也行：如三峡库区 GB-InSAR/GNSS 公开研究数据集）；
2. 把 GNSS 日解接进 `demo_arima_kalman.py`（降雨做外生），跑通在线纠偏原型；
3. 手工对齐一版"GNSS 3 站 + InSAR 速率场栅格"的宽表，跑通 ②GCN 的现有 demo；
4. 产出：一页对比图（GNSS 单点 vs InSAR 面域 vs 融合），作为立项材料。
